import React from 'react'
import Alert, { AlertProps, AlertType, AlertSize } from './yk-alert'
import from '@testing'
describe('Alert test', () => {
  it('test the props', () => {
    const wrapper = 
  })
})