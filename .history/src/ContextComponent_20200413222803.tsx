import React, { createContext } from 'react'

const ContextComponent: React.FC = () => {
  interface theme {
    [key: string]: {
      color: string;
      background: string;
    }
  }
}

export default ContextComponent;